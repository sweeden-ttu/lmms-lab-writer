name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-tauri-release:
    name: Release Build (${{ matrix.name }})
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
            target: ""
          - name: macOS (ARM64)
            os: macos-latest
            target: aarch64-apple-darwin
          - name: macOS (x64)
            os: macos-15-intel
            target: x86_64-apple-darwin
          - name: Windows
            os: windows-latest
            target: ""
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install system dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Prepare macOS bundle tools
        if: startsWith(matrix.os, 'macos')
        shell: bash
        run: |
          DEVELOPER_BIN="$(xcode-select -p)/usr/bin"
          echo "$DEVELOPER_BIN" >> "$GITHUB_PATH"
          echo "Using Xcode developer tools from: $DEVELOPER_BIN"
          xcrun --find SetFile
          xcrun --find Rez
          command -v hdiutil
          command -v osascript

      - run: pnpm install --frozen-lockfile

      - name: Sync app version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: node scripts/sync-version.mjs --from-tag ${{ github.ref_name }}

      - name: Build Tauri app
        run: pnpm --filter @lmms-lab/writer-desktop tauri build --ci -v ${{ matrix.target && format('--target {0}', matrix.target) }}

      - name: Build PKG installer (macOS release)
        if: startsWith(matrix.os, 'macos') && startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          TAURI_TARGET_TRIPLE: ${{ matrix.target }}
        run: ./scripts/build-pkg.sh

      - name: Upload artifacts
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.name }}
          path: |
            apps/desktop/src-tauri/target/**/release/bundle/**
          retention-days: 7

  build-aur-package:
    name: Prepare AUR Package Files
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: build-tauri-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-Linux
          path: aur-input

      - name: Generate PKGBUILD and .SRCINFO
        if: ${{ !contains(github.ref_name, '-') }}
        run: node scripts/generate-aur-bin-package.mjs --tag ${{ github.ref_name }} --linux-artifacts-dir aur-input --out-dir aur-dist/lmms-lab-writer-bin

      - name: Archive AUR package files
        if: ${{ !contains(github.ref_name, '-') }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p aur-dist
          tar -czf "aur-dist/lmms-lab-writer-bin-${{ github.ref_name }}-aur.tar.gz" -C aur-dist/lmms-lab-writer-bin .

      - name: Upload AUR package files
        if: ${{ !contains(github.ref_name, '-') }}
        uses: actions/upload-artifact@v4
        with:
          name: build-AUR
          path: |
            aur-dist/lmms-lab-writer-bin/**
            aur-dist/*.tar.gz
          retention-days: 7

  release-github:
    name: Publish GitHub Release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - build-tauri-release
      - build-aur-package
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: build-*
          path: release-artifacts

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          overwrite_files: true
          files: |
            release-artifacts/**/*.dmg
            release-artifacts/**/*.pkg
            release-artifacts/**/*.tar.gz
            release-artifacts/**/*.app.tar.gz
            release-artifacts/**/*.exe
            release-artifacts/**/*.msi
            release-artifacts/**/*.zip
            release-artifacts/**/*.deb
            release-artifacts/**/*.AppImage
            release-artifacts/**/*.rpm
            release-artifacts/**/*.sig
            release-artifacts/**/*.json
            release-artifacts/**/PKGBUILD
            release-artifacts/**/.SRCINFO
